#!/usr/bin/python

import numpy as np
import pandas as pd

class ReteNode():
    def __init__(self, name=None, prev=None, next=None, memory=df.DataFrame, pattern=None):
        self.name = name
        self.prev = prev
        self.next = next
        self.memory = memory
        self.pattern = pattern
